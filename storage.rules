rules_version = '2';

// Default-deny baseline (society-scoped paths added with the document features).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
