rules_version = '2';

// Default-deny baseline (S1.8 adds isMember / hasRole helpers + per-collection rules).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
